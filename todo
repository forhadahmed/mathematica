filter commands that can cause harm
warning to users if they set a variable that their setting will not be permanent
show "loading" gif when user presses enter for a computation
going up in history causes caret to be placed at the beginning of line (should be at end)
global settings tweaks
toggle between text output and rendered output
FIX BUGS!
